# Supabase Storage Buckets

Create these public buckets in Supabase Storage:

- `car-images`
- `profile-images`

The frontend `ImageUploader` writes to `car-images` for inventory photos. Profile uploads can use `profile-images` once the profile save endpoint is connected to your production account settings.
